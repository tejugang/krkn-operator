apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: krkn-operator-backups
  namespace: system
  labels:
    app.kubernetes.io/name: krkn-operator
    app.kubernetes.io/component: backup-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
